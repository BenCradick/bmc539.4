#!/bin/bash
#Ben Cradick
#cs 2750
#3-28-18

# function handleInterrupt {

# }
# function genNames {

# }

# awk 'BEGIN { FS=","} { print $2 " "  $1 }' names.txt |\
# sed "s/^[ \t]*//"  |\
# awk '{print $1 " " $NF}' |\
# sed 's/[^a-zA-Z ]//g' |\
# sed 's/\b\([[:alpha:]]\)\([[:alpha:]]*\)\b/\u\1\L\2/g' > names2.txt
collisions=0;
linecount= wc -l names2.txt

for i in `seq 1 $linecount`; do
    sed -e  "$ip" names2.txt
done

trap " { rm names2.txt } " SIGINT



